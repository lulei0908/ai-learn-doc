# 11 - 模型配置

## 可用模型

### 模型别名

| 别名 | 说明 |
|------|------|
| `default` | 推荐模型（根据账户类型自动选择） |
| `sonnet` | Claude Sonnet 4.6（日常编程任务） |
| `opus` | Claude Opus 4.6（复杂推理任务） |
| `haiku` | Claude Haiku（简单快速任务） |
| `sonnet[1m]` | Sonnet 100 万 token 上下文 |
| `opus[1m]` | Opus 100 万 token 上下文 |
| `opusplan` | 计划模式用 Opus，执行用 Sonnet |

**别名始终指向最新版本**。要固定版本，使用完整模型名。

### 默认模型行为

| 账户类型 | 默认模型 |
|---------|---------|
| Max 和 Team Premium | Opus 4.6 |
| Pro 和 Team Standard | Sonnet 4.6 |
| Enterprise | Opus 4.6 可用但非默认 |

## 设置模型

### 会话中切换

```
/model
# 显示模型选择器，使用方向键选择
/model sonnet
```

### 启动时指定

```bash
claude --model opus
claude --model sonnet
```

### 环境变量

```bash
export ANTHROPIC_MODEL=opus
claude
```

### 配置文件

```json
{
  "model": "opus"
}
```

## 努力级别（Effort Levels）

努力级别控制自适应推理，动态分配思考预算：

| 级别 | 说明 | 适用场景 |
|------|------|---------|
| `low` | 快速、便宜 | 简单直接任务 |
| `medium` | 平衡 | **推荐**，大多数编程任务 |
| `high` | 深度推理 | 复杂调试、架构决策 |
| `max` | 无约束（仅 Opus 4.6） | 极端复杂问题 |

### 设置努力级别

```
/effort low
/effort medium
/effort high
/effort max
/effort auto
```

```bash
claude --effort high
```

```bash
export CLAUDE_CODE_EFFORT_LEVEL=high
```

### 单次深度思考

```bash
> 分析这个复杂的并发问题
[在提示中包含 "ultrathink" 触发高努力级别]
```

## 扩展上下文

### 1M Token 上下文

Sonnet 4.6 和 Opus 4.6 支持 100 万 token 上下文窗口。

**可用性**：

| 计划 | Opus 4.6 1M | Sonnet 4.6 1M |
|------|-------------|---------------|
| Max, Team, Enterprise | 包含 | 包含 |
| Pro | 需要额外付费 | 需要额外付费 |
| API 按需付费 | 完整访问 | 完整访问 |

### 启用 1M 上下文

```bash
# 使用别名
/model opus[1m]
/model sonnet[1m]

# 使用完整模型名
/model claude-opus-4-6[1m]
```

## 模型选择指南

### 选择 Sonnet 当：

- 日常编码任务
- 快速文件编辑和创建
- 常规代码审查
- 标准重构任务

### 选择 Opus 当：

- 复杂架构决策
- 高级调试和问题诊断
- 需要深度推理的任务
- 处理大型代码库

### 选择 Haiku 当：

- 简单问题解答
- 快速代码搜索
- 基础文件操作
- 预算敏感场景

### 使用 opusplan 当：

- 需要计划和分析的工作
- 计划阶段用 Opus 推理
- 执行阶段用 Sonnet 效率更高

## 环境变量配置

### API 配置

```bash
export ANTHROPIC_API_KEY=sk-ant-api03-xxxxx
export ANTHROPIC_BASE_URL=https://api.anthropic.com
```

### 模型别名映射

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-6
export ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4-6
export ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-haiku-4-6
export CLAUDE_CODE_SUBAGENT_MODEL=sonnet
```

### Prompt Caching

```bash
export DISABLE_PROMPT_CACHING=1           # 禁用所有模型的缓存
export DISABLE_PROMPT_CACHING_HAIKU=1      # 仅禁用 Haiku
export DISABLE_PROMPT_CACHING_SONNET=1     # 仅禁用 Sonnet
export DISABLE_PROMPT_CACHING_OPUS=1       # 仅禁用 Opus
```

## 第三方部署

### Amazon Bedrock

```bash
export AWS_ACCESS_KEY_ID=your-key
export AWS_SECRET_ACCESS_KEY=your-secret
export AWS_REGION=us-east-1
export ANTHROPIC_BEDROCK_BASE_URL=https://bedrock-runtime.us-east-1.amazonaws.com
```

### Google Vertex AI

```bash
export CLOUD_ML_REGION=us-east5
export ANTHROPIC_VERTEX_PROJECT_ID=your-project-id
export ANTHROPIC_VERTEX_BASE_URL=https://us-east5-aiplatform.googleapis.com
```

### Microsoft Foundry

```bash
export ANTHROPIC_FOUNDRY_BASE_URL=https://your-foundry.azure.com
export ANTHROPIC_FOUNDRY_PROJECT=your-project
```

### 固定模型版本

```bash
# Bedrock
export ANTHROPIC_DEFAULT_OPUS_MODEL=us.anthropic.claude-opus-4-6-v1
export ANTHROPIC_DEFAULT_SONNET_MODEL=us.anthropic.claude-sonnet-4-6-v1

# Vertex AI
export ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-6
export ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4-6
```

## 自定义模型选项

添加单个自定义条目到模型选择器：

```bash
export ANTHROPIC_CUSTOM_MODEL_OPTION=my-gateway/claude-opus-4-6
export ANTHROPIC_CUSTOM_MODEL_OPTION_NAME=Opus via Gateway
export ANTHROPIC_CUSTOM_MODEL_OPTION_DESCRIPTION=内部 LLM 网关路由
```

## 企业配置

### 限制模型选择

```json
{
  "availableModels": ["sonnet", "haiku"]
}
```

### 组合配置

```json
{
  "model": "sonnet",
  "availableModels": ["sonnet", "haiku"]
}
```

### 模型覆盖

```json
{
  "modelOverrides": {
    "claude-opus-4-6": "arn:aws:bedrock:us-east-2:123456789012:application-inference-profile/opus-prod",
    "claude-sonnet-4-6": "arn:aws:bedrock:us-east-2:123456789012:application-inference-profile/sonnet-prod"
  }
}
```

## 模型能力

### Opus 4.6 / Sonnet 4.6 能力

| 能力 | 说明 |
|------|------|
| `effort` | 自适应推理 |
| `max_effort` | 最大努力级别 |
| `thinking` | 扩展思考 |
| `adaptive_thinking` | 动态分配思考 |
| `interleaved_thinking` | 工具间思考 |

### 声明能力

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL_NAME="Opus via Bedrock"
export ANTHROPIC_DEFAULT_OPUS_MODEL_DESCRIPTION="通过 Bedrock 自定义端点的 Opus"
export ANTHROPIC_DEFAULT_OPUS_MODEL_SUPPORTED_CAPABILITIES=effort,max_effort,thinking,adaptive_thinking,interleaved_thinking
```

## 查看当前模型

```
/status
```

显示：
- 当前模型
- 努力级别
- 账户信息

## 下一步

- 📚 学习 [常见工作流](./12-常见工作流.md)
- ✅ 查看 [最佳实践](./13-最佳实践.md)
