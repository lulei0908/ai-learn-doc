# 12 - 常见工作流

## 计划模式

在修改代码前先设计和审批计划。

### 进入计划模式

```
/plan
```

或：

```bash
claude --model opusplan
```

### 工作流程

1. Claude 进入只读模式，分析代码库
2. 制定详细计划
3. 展示计划供用户审批
4. 审批后退出计划模式，开始执行

### 使用场景

- 大规模重构
- 新功能实现
- 架构变更
- 需要预先审批的修改

### 退出计划模式

```
/unplan
```

## Git Worktrees 并行工作

使用 git worktrees 在隔离环境中并行执行多个任务。

### 手动创建

```bash
# 创建新 worktree
git worktree add ../my-project-feature-a feature-a

# 在新 worktree 中运行 Claude
cd ../my-project-feature-a
claude
```

### 使用子代理隔离

```yaml
---
name: isolated-task
description: 在隔离 worktree 中执行任务
isolation: worktree
---
```

## 扩展思考

### 单次启用

```
> 深入分析这个复杂的并发问题 ultrathink
```

### 配置启用

```
/thinking on
```

### 工作原理

- 模型使用更多思考 token
- 更深入的推理
- 适合复杂问题

## 上下文压缩

### 手动压缩

```
/compact
```

### 自动压缩

当上下文接近容量限制时自动触发。

### 配置压缩阈值

```bash
export CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50
```

## 项目初始化

### 自动初始化

```bash
cd new-project
claude
# Claude 会分析项目并建议创建 CLAUDE.md
```

### 手动初始化

```bash
claude /init
```

### 创建内容

- CLAUDE.md 项目概述
- 代码结构分析
- 主要依赖说明
- 编码规范建议

## PR 审查工作流

### 审查当前变更

```
> 审查当前的 PR 变更，关注：
> 1. 代码质量
> 2. 安全问题
> 3. 性能影响
> 4. 测试覆盖
```

### 创建 PR

```
> 为当前变更创建 PR，标题格式遵循约定式提交
```

### 使用 GitHub MCP

```bash
# 添加 GitHub MCP
claude mcp add --transport http github https://api.githubcopilot.com/mcp/
```

```
> 审查 PR #123 并提供改进建议
> 查看 PR #456 的评论并回应
```

## 测试驱动开发

### 编写测试优先

```
> 为用户认证功能编写测试，然后实现功能使测试通过
```

### 运行测试

```
> 运行 npm test，分析失败的测试并修复
```

### 测试覆盖

```
> 分析测试覆盖率，找出未覆盖的代码路径并添加测试
```

## 代码重构

### 提取函数

```
> 将这段重复的代码提取为可复用函数
```

### 模块拆分

```
> 将这个大文件拆分为多个模块，每个负责单一功能
```

### 模式迁移

```
> 将 Class 风格重构为函数式风格
> 将 Redux 迁移到 Zustand
```

## 调试工作流

### 错误分析

```
> 运行测试，分析错误堆栈，找出根本原因
```

### 日志分析

```
> 分析这些日志，找出问题的模式
```

### 性能问题

```
> 分析这个函数的性能瓶颈，提出优化方案
```

## 文档生成

### API 文档

```
> 为 src/api/ 中的所有端点生成 API 文档
```

### 代码注释

```
> 为这个复杂的函数添加详细注释
```

### README

```
> 为这个项目生成 README.md，包括：
> - 项目描述
> - 安装步骤
> - 使用说明
> - 配置选项
> - 贡献指南
```

## 依赖管理

### 检查更新

```
> 检查 package.json 中的依赖更新，分析主要版本变更的破坏性修改
```

### 安全审计

```
> 运行 npm audit，分析漏洞并提供修复方案
```

### 清理依赖

```
> 找出未使用的依赖并移除
```

## 代码迁移

### 语言迁移

```
> 将 src/ 下的 JavaScript 文件迁移到 TypeScript
```

### 框架迁移

```
> 将 Class 组件迁移为 React Hooks
> 将 Express 迁移到 Fastify
```

### 版本升级

```
> 将 React 17 升级到 React 18，处理所有破坏性修改
```

## 批量操作

### 使用 /batch

```
/batch 在 src/components 中添加 TypeScript 类型定义
```

### 并行子代理

```
> 使用三个并行子代理：
> 1. 审查 API 层
> 2. 审查服务层
> 3. 审查数据层
```

## 定时任务

### 使用 /loop

```
/loop 5m 检查部署状态，如果完成则通知我
```

### 使用 CronCreate

```
> 创建定时任务：每 10 分钟检查一次 CI 状态
```

## 多项目工作流

### 添加共享目录

```bash
claude --add-dir /shared/components --add-dir /shared/utils
```

### Monorepo 支持

Claude Code 自动发现子目录中的配置：

```
monorepo/
├── CLAUDE.md
├── packages/
│   ├── api/
│   │   └── CLAUDE.md
│   └── web/
│       └── CLAUDE.md
```

## 远程协作

### 使用 Channels

配置 MCP 服务器作为消息渠道，Claude 可响应外部事件。

### 共享会话

使用相同的项目配置，团队成员可复现相同的工作环境。

## CI/CD 集成

### 自动化脚本

```bash
#!/bin/bash
claude --model sonnet "运行测试并分析失败，输出修复建议" < /dev/null
```

### 非交互模式

```bash
claude --no-input "分析代码质量并输出报告" | tee report.md
```

## 下一步

- ✅ 查看 [最佳实践](./13-最佳实践.md)
