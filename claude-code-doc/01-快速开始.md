# 01 - 快速开始

## 安装 Claude Code

### 前置条件

- **Node.js** 18 或更高版本
- **Anthropic 账户**（Max、Pro、Team 或 Enterprise 订阅）
- **API 密钥** 或 **Claude.ai 登录认证**

### 安装方式

#### 方式一：npm 全局安装（推荐）

```bash
npm install -g @anthropic-ai/claude-code
```

#### 方式二：npx 直接运行（无需安装）

```bash
npx @anthropic-ai/claude-code
```

#### 方式三：升级已安装版本

```bash
npm update -g @anthropic-ai/claude-code
```

### 验证安装

```bash
claude --version
```

## 认证配置

### 方式一：Claude.ai 账户登录

最简单的方式，直接使用浏览器登录：

```bash
claude
# 首次运行会自动引导你登录 Claude.ai
```

### 方式二：API 密钥认证

```bash
# 设置环境变量
export ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# 然后启动
claude
```

### 方式三：Amazon Bedrock

```bash
export AWS_ACCESS_KEY_ID=your-key
export AWS_SECRET_ACCESS_KEY=your-secret
export AWS_REGION=us-east-1
export ANTHROPIC_BEDROCK_BASE_URL=https://bedrock-runtime.us-east-1.amazonaws.com

claude
```

### 方式四：Google Vertex AI

```bash
export CLOUD_ML_REGION=us-east5
export ANTHROPIC_VERTEX_PROJECT_ID=your-project-id
export ANTHROPIC_VERTEX_BASE_URL=https://us-east5-aiplatform.googleapis.com

claude
```

## 启动第一个会话

### 进入项目目录

```bash
cd your-project
claude
```

### 交互式使用

启动后你会看到 Claude Code 的提示符，可以直接输入自然语言：

```
> 解释这个项目的架构
> 在 src/utils 中添加一个日期格式化函数
> 修复 CI 中失败的测试
> 重构 auth 模块，使用新的 API 设计
```

### 单次命令模式

不进入交互式会话，直接执行单个任务：

```bash
# 执行单个提示并退出
claude "解释 package.json 中的依赖关系"

# 管道输入
cat error.log | claude "分析这些错误日志"

# 结合 stdin
echo "帮我写一个 Python HTTP 服务器" | claude
```

## 基本工作流

### 1. 代码生成

```
> 在 src/api/ 下创建一个 RESTful API 路由，支持 CRUD 操作
```

Claude 会：
1. 分析现有代码结构
2. 创建合适的文件
3. 编写完整的代码
4. 必要时请求权限确认

### 2. 代码审查

```
> 审查 src/auth/login.ts，检查安全问题
```

### 3. Bug 修复

```
> 运行测试，修复失败的用例
```

### 4. 重构代码

```
> 将 auth 模块从 Class 模式重构为函数式风格
```

### 5. 项目初始化

Claude Code 首次在新项目中运行时会自动生成 `CLAUDE.md` 文件：

```bash
claude
# Claude 会分析项目并提出创建 CLAUDE.md
```

## 重要概念

### 工作目录

Claude Code 在启动时的工作目录决定了它的项目范围：

```bash
# 在项目根目录启动
cd /path/to/my-project
claude

# 或使用 --cd 参数
claude --cd /path/to/my-project
```

### 添加额外目录

如果需要访问项目目录之外的文件：

```bash
# 启动时添加
claude --add-dir /path/to/other/project

# 会话中添加
/add-dir /path/to/other/project
```

### 权限系统

Claude Code 使用分层权限控制：

| 工具类型 | 示例 | 需要批准 |
|---------|------|---------|
| 只读操作 | 文件读取、搜索 | 否 |
| Bash 命令 | Shell 执行 | 是 |
| 文件修改 | 编辑/写入文件 | 是 |

### 会话管理

```bash
# 恢复上次会话
claude --continue

# 从最近的会话中选择恢复
claude --resume

# 启动新会话
claude --new
```

## 常见操作示例

### 搜索代码

```
> 找到所有使用 deprecated API 的地方
> 搜索项目中所有的 TODO 注释
```

### 运行命令

```
> 运行 npm test 并分析失败的测试
> 执行 git status，看看有哪些未提交的更改
```

### 网络搜索

```
> 搜索 React 19 的新特性
> 查找如何配置 Nginx 反向代理
```

### 文件操作

```
> 读取 src/config/database.ts 的内容
> 在 src/utils 中创建一个新的工具函数文件
> 修改 API 响应格式，统一使用 snake_case
```

## 下一步

- 📖 了解所有 [CLI 命令参考](./02-CLI命令参考.md)
- 🎮 探索 [交互模式](./03-交互模式.md)
- 🔧 配置 [记忆系统](./05-记忆系统.md) 让 Claude 更好地理解你的项目
