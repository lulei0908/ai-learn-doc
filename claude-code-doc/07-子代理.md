# 07 - 子代理

## 概述

子代理（Subagents）是专业化的 AI 助手，在独立的上下文窗口中运行，具有自定义的系统提示、特定的工具访问权限和独立的权限控制。当 Claude 遇到匹配的任务时，会委托给子代理处理。

## 内置子代理

### Explore

快速、只读的代理，优化用于搜索和分析代码库。

- **模型**：Haiku（快速、低延迟）
- **工具**：只读工具（禁用 Write 和 Edit）
- **用途**：文件发现、代码搜索、代码库探索

### Plan

用于计划模式的研究代理，在展示计划前收集上下文。

- **模型**：继承自主对话
- **工具**：只读工具
- **用途**：规划时的代码库研究

### General-purpose

用于复杂、多步骤任务的通用代理。

- **模型**：继承自主对话
- **工具**：所有工具
- **用途**：复杂研究、多步骤操作、代码修改

### 其他内置代理

| 代理 | 模型 | 用途 |
|------|------|------|
| Bash | 继承 | 在独立上下文中运行终端命令 |
| statusline-setup | Sonnet | 配置状态栏 |
| Claude Code Guide | Haiku | 回答 Claude Code 功能问题 |

## 创建自定义子代理

### 方式一：使用 /agents 命令

```bash
/agents
# 选择 "Create new agent"
# 选择 "Personal"（个人）或 "Project"（项目）
# 选择 "Generate with Claude" 让 AI 生成配置
# 或手动配置
```

### 方式二：手动创建文件

#### 项目级子代理

```bash
mkdir -p .claude/agents
```

创建 `.claude/agents/code-reviewer.md`：

```yaml
---
name: code-reviewer
description: 代码审查专家，主动审查代码质量、安全性和可维护性
tools: Read, Grep, Glob, Bash
model: inherit
---

你是高级代码审查员，确保代码质量和安全高标准。

当被调用时：
1. 运行 git diff 查看最近变更
2. 重点审查修改的文件
3. 立即开始审查

审查清单：
- 代码清晰易读
- 函数和变量命名良好
- 无重复代码
- 适当的错误处理
- 无暴露的密钥
- 输入验证已实现
- 测试覆盖充分
- 考虑了性能

按优先级组织反馈：
- 关键问题（必须修复）
- 警告（应该修复）
- 建议（考虑改进）

包含具体的修复示例。
```

#### 个人级子代理

```bash
mkdir -p ~/.claude/agents
```

创建 `~/.claude/agents/debugger.md`：

```yaml
---
name: debugger
description: 调试专家，用于错误、测试失败和异常行为
tools: Read, Edit, Bash, Grep, Glob
---

你是专业调试器，专精根因分析。

当被调用时：
1. 捕获错误信息和堆栈跟踪
2. 确定复现步骤
3. 隔离故障位置
4. 实现最小化修复
5. 验证解决方案

调试过程：
- 分析错误信息和日志
- 检查最近的代码变更
- 形成并测试假设
- 添加战略性调试日志
- 检查变量状态

对于每个问题提供：
- 根因解释
- 支持诊断的证据
- 具体的代码修复
- 测试方法
- 预防建议

专注于修复根本问题，而非症状。
```

## 前置元数据字段

| 字段 | 必需 | 说明 |
|------|------|------|
| `name` | ✅ | 唯一标识符（小写字母和连字符） |
| `description` | ✅ | Claude 决定何时委托的依据 |
| `tools` | ❌ | 可用工具列表（默认继承所有） |
| `disallowedTools` | ❌ | 禁用的工具列表 |
| `model` | ❌ | 模型选择（sonnet/opus/haiku/inherit） |
| `permissionMode` | ❌ | 权限模式 |
| `maxTurns` | ❌ | 最大代理轮次 |
| `skills` | ❌ | 预加载的技能列表 |
| `mcpServers` | ❌ | 子代理专用的 MCP 服务器 |
| `hooks` | ❌ | 子代理特定的钩子 |
| `memory` | ❌ | 持久化内存作用域 |
| `background` | ❌ | 是否作为后台任务运行 |
| `effort` | ❌ | 努力级别 |
| `isolation` | ❌ | `worktree` 使用 git worktree 隔离 |
| `initialPrompt` | ❌ | 自动提交的第一个提示 |

## 子代理位置与优先级

| 来源 | 位置 | 优先级 |
|------|------|--------|
| CLI 定义 | `--agents` 参数 | 最高 |
| 项目 | `.claude/agents/` | 2 |
| 个人 | `~/.claude/agents/` | 3 |
| 插件 | `<plugin>/agents/` | 最低 |

## CLI 定义子代理

```bash
claude --agents '{
  "code-reviewer": {
    "description": "代码审查专家",
    "prompt": "你是高级代码审查员...",
    "tools": ["Read", "Grep", "Glob", "Bash"],
    "model": "sonnet"
  },
  "debugger": {
    "description": "调试专家",
    "prompt": "你是专业调试器..."
  }
}'
```

## 工具访问控制

### 允许特定工具

```yaml
---
name: safe-researcher
description: 受限的研究代理
tools: Read, Grep, Glob, Bash
---
```

### 禁用特定工具

```yaml
---
name: no-writes
description: 继承所有工具但禁用文件写入
disallowedTools: Write, Edit
---
```

### 限制可生成的子代理

```yaml
---
name: coordinator
description: 协调工作
tools: Agent(worker, researcher), Read, Bash
---
```

## 权限模式

| 模式 | 行为 |
|------|------|
| `default` | 标准权限检查，有提示 |
| `acceptEdits` | 自动接受文件编辑 |
| `dontAsk` | 自动拒绝未预授权的工具 |
| `bypassPermissions` | 跳过权限提示 |
| `plan` | 计划模式（只读探索） |

**警告**：`bypassPermissions` 需谨慎使用。

## MCP 服务器隔离

为子代理配置专用的 MCP 服务器：

```yaml
---
name: browser-tester
description: 使用 Playwright 测试浏览器功能
mcpServers:
  - playwright:
      type: stdio
      command: npx
      args: ["-y", "@playwright/mcp@latest"]
  - github
---

使用 Playwright 工具进行导航、截图和页面交互。
```

## 预加载技能

```yaml
---
name: api-developer
description: 按照团队规范实现 API 端点
skills:
  - api-conventions
  - error-handling-patterns
---

实现 API 端点，遵循预加载技能中的规范和模式。
```

## 持久化内存

```yaml
---
name: code-reviewer
description: 代码审查专家
memory: user
---

你是代码审查员。随着审查代码，更新你的代理内存，记录发现的模式、约定和常见问题。

内存位置：~/.claude/agent-memory/code-reviewer/
```

### 内存作用域

| 作用域 | 位置 | 用途 |
|--------|------|------|
| `user` | `~/.claude/agent-memory/<name>/` | 跨项目记忆 |
| `project` | `.claude/agent-memory/<name>/` | 项目特定，可共享 |
| `local` | `.claude/agent-memory-local/<name>/` | 项目特定，不提交 |

## 钩子定义

```yaml
---
name: db-reader
description: 执行只读数据库查询
tools: Bash
hooks:
  PreToolUse:
    - matcher: "Bash"
      hooks:
        - type: command
          command: "./scripts/validate-readonly-query.sh"
---
```

## 调用子代理

### 自然语言

```
> 使用 code-reviewer 子代理审查最近的变更
> 让 debugger 子代理看看这个问题
```

### @ 提及

```
> @"code-reviewer (agent)" 审查认证模块的变更
```

### 整个会话作为子代理

```bash
# 启动会话，使用子代理的系统提示
claude --agent code-reviewer

# 在配置中设置默认
```

```json
{
  "agent": "code-reviewer"
}
```

## 后台运行

```yaml
---
name: background-task
description: 后台执行任务
background: true
---
```

或请求时指定：

```
> 在后台运行这个任务
```

快捷键 `Ctrl+B` 可将正在运行的任务移至后台。

## Git Worktree 隔离

```yaml
---
name: isolated-worker
description: 在隔离环境中工作
isolation: worktree
---
```

子代理会在临时 git worktree 中运行，完成后自动清理。

## 禁用特定子代理

```json
{
  "permissions": {
    "deny": ["Agent(Explore)", "Agent(my-custom-agent)"]
  }
}
```

或通过 CLI：

```bash
claude --disallowedTools "Agent(Explore)"
```

## 使用模式

### 隔离大输出操作

```
> 使用子代理运行测试套件，只报告失败的测试和错误信息
```

### 并行研究

```
> 使用独立子代理并行研究认证、数据库和 API 模块
```

### 链式子代理

```
> 先用 code-reviewer 子代理发现性能问题，再用 optimizer 子代理修复
```

## 恢复子代理

子代理完成后，可以恢复继续工作：

```
> 使用 code-reviewer 子代理审查认证模块
[子代理完成]

> 继续那个审查，分析授权逻辑
[Claude 恢复子代理，保留完整上下文]
```

## 子代理 vs 主会话

**使用主会话：**
- 需要频繁交互或迭代优化
- 多个阶段共享大量上下文
- 快速、针对性修改
- 延迟敏感

**使用子代理：**
- 产生大量输出但只需摘要
- 需要特定工具限制或权限
- 工作独立，可返回摘要

## 下一步

- 🔗 集成 [MCP](./08-MCP集成.md)
- 🪝 配置 [Hooks](./09-Hooks系统.md)
