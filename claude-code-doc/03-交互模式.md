# 03 - 交互模式

## 内置斜杠命令

在 Claude Code 会话中，可以使用斜杠命令执行各种操作。

### 会话管理

| 命令 | 说明 |
|------|------|
| `/help` | 显示帮助信息 |
| `/clear` | 清除会话历史，重新开始 |
| `/compact` | 压缩对话历史以释放上下文空间 |
| `/cost` | 显示当前会话的费用统计 |
| `/status` | 显示会话状态和账户信息 |
| `/quit` 或 `/exit` | 退出 Claude Code |

### 模型与配置

| 命令 | 说明 |
|------|------|
| `/model` | 切换模型（显示模型选择器） |
| `/effort <level>` | 设置努力级别（low, medium, high, max, auto） |
| `/permissions` | 管理权限规则 |
| `/config` | 打开设置文件编辑 |

### 文件与目录

| 命令 | 说明 |
|------|------|
| `/add-dir <path>` | 添加额外可访问目录 |
| `/remove-dir <path>` | 移除已添加的目录 |
| `/ls` | 列出当前目录内容 |
| `/read <file>` | 读取文件内容（等同于 @file） |

### MCP 管理

| 命令 | 说明 |
|------|------|
| `/mcp` | 管理 MCP 服务器，查看状态，进行认证 |
| `/mcp-add <name> -- <command>` | 添加 MCP 服务器 |
| `/mcp-remove <name>` | 移除 MCP 服务器 |

### 代理管理

| 命令 | 说明 |
|------|------|
| `/agents` | 管理子代理（查看、创建、编辑、删除） |
| `/agent <name>` | 使用指定子代理 |

### 技能与 Hooks

| 命令 | 说明 |
|------|------|
| `/skills` | 管理技能 |
| `/hooks` | 查看已配置的 Hooks |

### 其他命令

| 命令 | 说明 |
|------|------|
| `/init` | 在当前项目初始化 Claude Code |
| `/doctor` | 诊断环境问题 |
| `/bug` | 报告问题 |
| `/vim` | 启用 vim 模式编辑 |
| `/terminal-setup` | 设置终端集成 |
| `/ide` | 打开 IDE 设置 |
| `/pr-comments` | 查看 PR 评论 |
| `/btw` | 上下文内提问（不保存到历史） |
| `/notebook` | 管理 Jupyter notebook |

### 计划模式

| 命令 | 说明 |
|------|------|
| `/plan` | 进入计划模式 |
| `/unplan` | 退出计划模式 |

## 快捷键

### 基本操作

| 快捷键 | 说明 |
|--------|------|
| `Ctrl+C` | 中断当前操作 |
| `Ctrl+D` | 退出 Claude Code |
| `Ctrl+L` | 清屏 |
| `Enter` | 提交输入 |
| `↑/↓` | 浏览历史输入 |

### 编辑操作

| 快捷键 | 说明 |
|--------|------|
| `Ctrl+A` | 移动到行首 |
| `Ctrl+E` | 移动到行尾 |
| `Ctrl+K` | 删除从光标到行尾 |
| `Ctrl+U` | 删除从行首到光标 |
| `Ctrl+W` | 删除前一个单词 |
| `Ctrl+Y` | 粘贴删除的内容 |

### Vim 模式（启用后）

| 快捷键 | 说明 |
|--------|------|
| `Esc` | 进入普通模式 |
| `i` | 进入插入模式 |
| `a` | 在光标后插入 |
| `o` | 在下方新建行 |
| `O` | 在上方新建行 |
| `dd` | 删除行 |
| `yy` | 复制行 |
| `p` | 粘贴 |

### 多行输入

| 快捷键 | 说明 |
|--------|------|
| `Shift+Enter` | 插入换行（不提交） |
| `Alt+Enter` | 插入换行 |

### 特殊操作

| 快捷键 | 说明 |
|--------|------|
| `Ctrl+B` | 将当前任务移至后台 |
| `Ctrl+O` | 显示详细输出（verbose 模式） |
| `Ctrl+R` | 反向搜索历史 |

## @ 提示符（文件引用）

使用 `@` 符号引用文件、目录或 MCP 资源。

### 引用文件

```
> 分析 @src/auth/login.ts 的安全问题
> 比较 @src/api/v1/ 和 @src/api/v2/ 的差异
> 重构 @src/utils/helpers.js
```

### 引用目录

```
> 分析 @src/ 的架构
> 查看 @tests/ 的测试覆盖情况
```

### 引用 MCP 资源

```
> 分析 @github:issue://123 的问题描述
> 查看 @docs:file://api/authentication
```

### 引用 URL

```
> 获取 @https://example.com/api-docs 的内容
```

### 多文件引用

```
> 比较 @src/a.ts 和 @src/b.ts 的实现
> 合并 @config/base.json 和 @config/prod.json
```

## Tab 自动补全

Claude Code 支持智能自动补全：

### 补全类型

- **文件路径** - 输入路径时自动补全文件名
- **命令** - 输入 `/` 时显示可用命令
- **技能** - 输入 `/` 时显示自定义技能
- **@ 引用** - 输入 `@` 时显示可引用的资源

### 使用示例

```bash
# 文件路径补全
> 编辑 @src/ut<Tab>  → @src/utils/

# 命令补全
> /mod<Tab>  → /model

# 技能补全
> /deploy<Tab>  → /deploy（如果有该技能）
```

## 交互式选择器

### 文件选择器

某些命令会打开交互式文件选择器：

```
> /read
# 显示文件列表，可选择要读取的文件
```

### 模型选择器

`/model` 命令打开模型选择界面：

```
> /model
# 显示可选模型列表，使用方向键选择
# 可用左右键调整努力级别
```

### 权限管理

`/permissions` 打开权限管理界面：

```
> /permissions
# 显示所有权限规则
# 可添加、修改、删除规则
```

## 特殊语法

### 代码块引用

在提示中使用代码块：

```
> 解释这段代码：
```typescript
function processData(input: string): Result {
  // ...
}
```
```

### 多步骤任务

```
> 第一步：分析代码结构
> 第二步：识别潜在问题
> 第三步：提出改进方案
```

### 条件指令

```
> 如果测试失败，分析错误原因
> 如果文件不存在，创建新文件
```

### 上下文限制

```
> 只关注 src/api 目录
> 忽略测试文件
> 使用最多 50000 tokens
```

## 会话持久化

### 自动保存

Claude Code 自动保存会话历史：

- 位置：`~/.claude/projects/<project-hash>/<session-id>/`
- 格式：JSONL 文件
- 保存周期：默认 30 天

### 恢复会话

```bash
# 继续上次会话
claude --continue

# 选择历史会话
claude --resume

# 恢复特定会话
claude --session abc123
```

### 清理历史

```json
// 在 settings.json 中配置清理周期
{
  "cleanupPeriodDays": 30
}
```

## 后台任务

### 创建后台任务

```
> 在后台运行 npm test
> 使用后台代理分析整个代码库
```

或使用快捷键 `Ctrl+B` 将正在运行的任务移至后台。

### 管理后台任务

```
> 列出后台任务
> 检查后台任务状态
> 停止后台任务
```

### 禁用后台任务

```bash
export CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1
claude
```

## 侧边提问 (/btw)

使用 `/btw` 进行上下文内提问，不影响主对话历史：

```
> /btw 什么是 prompt caching？
# Claude 回答后继续主对话
```

这个功能适合：
- 快速查询概念
- 临时获取帮助
- 不需要保存的问答

## 下一步

- 📖 了解 [工具参考](./04-工具参考.md)
- 🧠 配置 [记忆系统](./05-记忆系统.md)