# 13 - 最佳实践

## 项目配置

### 创建优质的 CLAUDE.md

```markdown
# 项目名称

## 概述
简洁描述项目目的和核心功能。

## 技术栈
- 前端：React 18 + TypeScript
- 后端：Node.js + Express
- 数据库：PostgreSQL
- 部署：Docker + Kubernetes

## 架构
描述主要模块和它们的职责：
- src/api/ - REST API 路由
- src/services/ - 业务逻辑
- src/models/ - 数据模型
- src/utils/ - 工具函数

## 编码规范
- 使用 2 空格缩进
- 组件使用 PascalCase
- 函数使用 camelCase
- 导入顺序：外部 → 内部 → 类型

## 常用命令
- npm run dev - 开发服务器
- npm test - 运行测试
- npm run build - 生产构建

## 注意事项
- 不要修改 src/legacy 目录
- API 响应使用 snake_case
- 所有 API 调用必须处理错误
```

### 分离关注点

```
project/
├── CLAUDE.md              # 通用项目信息
├── CLAUDE.local.md        # 本地私有配置（gitignore）
└── src/
    ├── api/
    │   └── CLAUDE.md      # API 特定规范
    └── components/
        └── CLAUDE.md      # 组件特定规范
```

## 高效沟通

### 清晰的任务描述

```
# 好的做法 ✅
> 在 src/api/users.ts 中添加 PATCH /users/:id 端点，
> 支持更新用户名和邮箱，需要验证邮箱格式，
> 返回更新后的用户对象

# 不好的做法 ❌
> 加个更新用户的 API
```

### 分步骤复杂任务

```
# 好的做法 ✅
> 实现用户认证功能：
> 1. 创建用户模型和数据库迁移
> 2. 实现注册和登录 API
> 3. 添加 JWT 认证中间件
> 4. 编写测试用例

# 不好的做法 ❌
> 实现用户认证
```

### 提供上下文

```
# 好的做法 ✅
> 重构 src/services/payment.ts 中的 processPayment 函数，
> 当前有 300 行代码，需要拆分为更小的函数，
> 保持向后兼容，确保现有测试通过

# 不好的做法 ❌
> 重构 payment 服务
```

## 权限管理

### 最小权限原则

```json
{
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(git status)",
      "Bash(git diff *)",
      "Read",
      "Edit(/src/**)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push * main)",
      "Read(./.env)",
      "Edit(./.env)"
    ]
  }
}
```

### 开发环境配置

```json
{
  "permissions": {
    "allow": [
      "Bash(npm *)",
      "Bash(git *)",
      "Bash(node *)",
      "Read",
      "Edit"
    ]
  },
  "defaultMode": "default"
}
```

### CI/CD 环境配置

```json
{
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Read",
      "Edit(/src/**)"
    ],
    "deny": [
      "Bash(git push *)"
    ]
  },
  "defaultMode": "acceptEdits"
}
```

## 性能优化

### 使用子代理隔离大输出

```
> 使用子代理运行完整测试套件，只报告失败的测试
```

### 并行研究

```
> 使用三个并行子代理分别研究：
> 1. 认证模块的实现
> 2. 数据库查询的性能
> 3. API 响应的格式
```

### 定期压缩

```
/compact
```

## 技能和代理

### 创建有用的技能

```yaml
---
name: code-review
description: 审查代码质量和最佳实践
---

## 审查清单

### 代码质量
- [ ] 命名清晰有意义
- [ ] 函数单一职责
- [ ] 无重复代码

### 安全
- [ ] 无硬编码凭据
- [ ] 输入已验证
- [ ] 输出已编码

### 测试
- [ ] 单元测试覆盖
- [ ] 边界情况处理
- [ ] 错误路径测试
```

### 配置专用子代理

```yaml
---
name: db-migrator
description: 数据库迁移专家
tools: Read, Edit, Bash
allowed-tools: Bash(npx prisma *)
---

处理数据库迁移：
1. 分析当前 schema
2. 创建迁移文件
3. 验证迁移正确性
4. 生成回滚脚本
```

## MCP 集成

### 常用 MCP 配置

```bash
# GitHub
claude mcp add --transport http github https://api.githubcopilot.com/mcp/

# 文件系统
claude mcp add --transport stdio filesystem -- npx -y @modelcontextprotocol/server-filesystem .

# 数据库
claude mcp add --transport stdio db -- npx -y @bytebase/dbhub --dsn "$DATABASE_URL"
```

### 安全考虑

- 仅添加信任的 MCP 服务器
- 注意处理不可信内容的 MCP 工具（可能暴露于提示注入）
- 使用权限规则限制敏感操作

## Hooks 自动化

### 自动格式化

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npm run format"
          }
        ]
      }
    ]
  }
}
```

### 安全检查

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(rm *)",
            "command": "./scripts/check-rm.sh"
          }
        ]
      }
    ]
  }
}
```

## 错误处理

### 当 Claude 卡住时

1. 使用 `Ctrl+C` 中断
2. 提供更明确的指令
3. 使用 `/compact` 释放上下文
4. 拆分为更小的任务

### 当结果不满意时

1. 提供更具体的反馈
2. 展示期望的输出示例
3. 分步骤重新开始
4. 使用不同的模型（如 Opus）

## 模型选择

### 按任务选择

| 任务类型 | 推荐模型 | 原因 |
|---------|---------|------|
| 日常编码 | Sonnet | 平衡性能和成本 |
| 复杂调试 | Opus | 需要深度推理 |
| 简单任务 | Haiku | 快速且便宜 |
| 架构设计 | Opus + 计划模式 | 复杂决策 |

### 使用 opusplan

```bash
claude --model opusplan
```

- 计划阶段自动使用 Opus
- 执行阶段自动切换 Sonnet

## 安全最佳实践

### 敏感信息

- 不要将 API 密钥写入代码
- 使用 `.env` 文件存储敏感信息
- 配置 Read/Edit 规则保护敏感文件

```json
{
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Edit(./.env)",
      "Edit(./.env.*)"
    ]
  }
}
```

### 生产环境

- 使用托管设置控制配置
- 禁用危险模式
- 限制 MCP 服务器访问

```json
{
  "allowManagedPermissionRulesOnly": true,
  "disableBypassPermissionsMode": "disable"
}
```

## 团队协作

### 共享配置

```bash
# 提交项目配置
git add .claude/
git add .mcp.json
git add CLAUDE.md

# 忽略本地私有配置
echo "CLAUDE.local.md" >> .gitignore
echo ".claude/settings.local.json" >> .gitignore
```

### 技能共享

将常用工作流打包为技能，放在项目的 `.claude/skills/` 目录中。

## 持续改进

### 更新 CLAUDE.md

- 定期更新项目架构信息
- 添加新的编码规范
- 记录重要决策

### 监控成本

```
/cost
```

定期检查使用情况，优化提示效率。

### 反馈循环

- 保存成功的提示模式
- 记录遇到的问题
- 更新技能和配置

---

## 总结

1. **清晰的沟通** - 提供明确的任务描述和上下文
2. **合理的权限** - 最小权限原则，保护敏感信息
3. **高效的组织** - 使用子代理隔离，并行执行任务
4. **自动化工作流** - 利用 Hooks 和技能简化重复任务
5. **持续改进** - 更新配置，优化提示，分享最佳实践

---

*文档完成！*

**文档版本**：1.0
**最后更新**：2026-03-29
**基于**：Claude Code 官方文档
